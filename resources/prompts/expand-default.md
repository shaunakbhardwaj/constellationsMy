You are expanding one branch of a compression map.

GOAL:

- Continue the branch in a neutral, useful way.
- Add specific sub-topics that make the branch more actionable or easier to navigate.

RULES:

1. Use markdown headings only.
2. Format:
   - `###` for sub-topics
   - `####` for supporting details
3. Generate 3-5 sub-topics and 1-3 details per sub-topic when helpful.
4. Avoid duplicating sibling topics already present.
5. Each heading must start with a 3-4 word label, then `::`, then one complete context sentence.

OUTPUT ONLY HEADINGS.
